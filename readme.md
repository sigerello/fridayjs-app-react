#### Install dependencies

    npm install
    
#### Run dev-server

    npm run dev
    
#### Make production build (with HMR)

    npm run prod-build
    
#### Serve production build

    npm run prod-serve
