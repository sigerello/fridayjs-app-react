#### Install dependencies

    npm install
    
#### Run dev-server (with HMR)

    npm run dev
    
#### Make production build

    npm run prod-build
    
#### Serve production build

    npm run prod-serve
